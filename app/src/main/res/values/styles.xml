<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Application Theme must have parent of AppCompat NoActionbar or else the stock actionbar will be displayed -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
    </style>

    <style name="Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">?attr/colorPrimary</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:layout_width">match_parent</item>
        <item name="theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
    </style>

    <style name="RecyclerView">
        <item name="android:scrollbars">none</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:paddingTop">@dimen/activity_vertical_margin</item>
        <item name="android:paddingBottom">@dimen/activity_vertical_margin</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/activity_horizontal_margin</item>
        <item name="android:clipToPadding">false</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="CardView" parent="CardView.Light">
        <item name="android:layout_margin">@dimen/cardview_space_margin</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:padding">@dimen/list_item_padding_all</item>
        <item name="cardCornerRadius">@dimen/cardview_radius</item>
        <item name="elevation">@dimen/cardview_default_elevation</item>
    </style>

    <style name="ListTitle" parent="android:TextAppearance.Large">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">@dimen/list_item_title_padding</item>
    </style>

    <style name="ListDescription" parent="android:TextAppearance.Medium">
        <item name="android:textColor">@android:color/darker_gray</item>
        <item name="android:paddingLeft">@dimen/list_item_description_padding</item>
        <item name="android:paddingRight">@dimen/list_item_description_padding</item>
        <item name="android:paddingBottom">@dimen/list_item_description_padding</item>
    </style>

</resources>
